package main

func main() {
	// weight, height, tire
	if !weight {
		return error because !weight
	}

	if !height {
		return error because !height
	}

	if !tire {
		return error b/e !tire
	}

	// all condidions are met
	return result
}
